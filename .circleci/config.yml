version: 2.1
executors:
  exectr:
    docker:
      - image: dockerben/cpptemplate:latest
jobs:
  test:
    executor: exectr
    steps:
      - checkout
      - run: sudo apt install ruby
      - run:
          name: test
          command: |
            mkdir build
            cd build
            cmake ..
            make MATEKF411TE
workflows:
  test_my_app:
    jobs:
      - test