version: 2.1
orbs:
  ruby: circleci/ruby@1.1
executors:
  exectr:
    docker:
      - image: dockerben/cpptemplate:latest
jobs:
  test:
    executor: exectr
    steps:
      - checkout
      - ruby/install:
          version: '2.7'      
      #- run: sudo apt install ruby
      - checkout
      - run:
          name: test
          command: |
            mkdir build
            cd build
            cmake ..
            make MATEKF411TE
workflows:
  test_my_app:
    jobs:
      - test